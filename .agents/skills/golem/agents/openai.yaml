interface:
  display_name: "Golem"
  short_description: "Repo-specific patterns and workflows for golem"
  default_prompt: "Use the golem repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true